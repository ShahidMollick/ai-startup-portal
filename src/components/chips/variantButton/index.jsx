import React from 'react';
import './index.css'; // Ensure this import is correct

const VariantButton = ({ text, onRemove }) => {
  return (
    <button className="variant-button">
      <span>Add more information section</span>
    </button>
  );
};

export default VariantButton;
